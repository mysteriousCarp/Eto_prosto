### Формат функции

```javascript
function name(config = {}) {
    // config может содержать любые пары ключ-значение
    
    return { 
        result: 0, // 0 or 1
        text: ""   // msg
    };
}